# 需求：文件浏览框改为"点浏览按钮弹出选择器"（对齐 solo-agent-kit 交互，不摊开示例）

> 用户反馈：点击"选择文件"还是空；当前实现把示例数据摊开在界面上，跟 solo-agent-kit 不同。
> solo-agent-kit 的正确交互：点「浏览」按钮 → 弹出文件选择器（默认在示例目录）→ 点选文件确认。**不摊开示例列表在界面上**。

## 参考（solo-agent-kit web/app.js 244-257）
- `browseFile()`：点浏览按钮 → 调 /api/browse → 渲染文件列表（默认示例目录）→ 点选 selectFile 填入
- 默认根 = examples/data（示例数据直接可见）
- **不摊开**：只有点「浏览」才显示文件列表

## 现状（factory-ontology，问题）
- 数据建模 tab **一进页面就 loadBrowse() 加载并摊开**浏览框文件列表（83行 loadBrowse + 620行一直显示"浏览数据文件"）
- 文件列表始终显示在界面上（摊开），不是"点击才弹出"

## 目标文件
- `E:\open-source\factory-ontology-kit\web\src\App.svelte`（数据建模 tab 数据源输入区）

## 要求（对齐 solo-agent-kit 交互）
1. **改为"点浏览按钮弹出"**：数据源输入区只放一个「📂 浏览数据文件」按钮（+ 当前选中文件显示）
2. **点击按钮 → 展开/弹出文件选择器**（默认在 data_valve 示例目录），显示文件列表（📄 文件名 + 路径）+ 目录导航（上级/子目录）
3. **点选文件确认**：点选多个文件 → 显示已选 → 「确认并建模（N 个文件）」→ setupOntologyMulti
4. **收起**：选择完成或点其他处，选择器收起（不一直摊开在界面）
5. 保留：系统文件选择 / 用默认示例建模 / DB接入
6. 移除"一进页面就摊开浏览列表"（不再 loadBrowse 自动展开）

## 约束
- Svelte 5（$state/$props）
- 极简：复用现有 browseFiles/readDataFile/setupOntologyMulti
- 浅色工业风
- 不破坏：系统文件选择/DB接入/问答/看板

## 验收
1. 数据建模 tab 不再摊开浏览列表（只有"浏览数据文件"按钮）
2. 点「浏览数据文件」→ 弹出文件选择器（默认 data_valve 示例目录）显示文件
3. 点选多文件 → 确认并建模
4. 不点时不显示列表
5. vite build 无 error
6. pytest 29 passed
