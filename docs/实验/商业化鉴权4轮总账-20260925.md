# 商业化鉴权 4 轮总账（2026-09-25 早上交）

仓库：`D:/factory-ontology`（main）
状态：**21 个文件改动全部未提交，HEAD 仍 `5dd7f5b`**（按要求压到今早，等你点头）
上游：远端 main 已同步至 `5dd7f5b`（昨晚那批：改造七批 + 开源脱敏）

## 一句话

你点的「商业化鉴权 1–6」四项都做完了：**多租户与行级隔离、本体端点严格鉴权开关 + 上传上限、令牌吊销/刷新 + 限速防爆破、SSO 接入适配**。四轮全部串行、每轮都过了自己的自检 + 8 道门 + 真实 HTTP，**本体资产零改动**（你放宽了口径，但没有方案需要动它），新模块**没引任何第三方依赖**。

## 四轮明细

| 轮 | 做了什么 | 新增/改动 | 自检（我亲手复跑） | 关键证据 |
|---|---|---|---|---|
| 1 | **多租户 + 行级隔离**：租户解析（凭据声明 > `X-Tenant-Id` > 默认 `default`）、KB 按租户可见、数据访问层强制租户条件、缺上下文 fail-closed、审计双通道带 tenant | `codes/tenant.py`、`codes/config/tenants.json` 新；`api_server/db_dialect/db_loader/audit_chain/kb_registry` 改 | `verify_tenant` **87/87** | 两套凭据真实 HTTP 各一次：A 只见自己的 KB；越权**被拒（不是空列表）**；默认租户 KB 列表集合改前改后 **33=33 完全相同** |
| 2 | **严格鉴权开关 + 上传上限**：`FOOD_STRICT_AUTH=1` 时本体结构/图等匿名端点也要凭据（无凭据 401、越权 403）；上传加上限（可配，默认 50MB），超限 **413 且不落盘**，读入有界 | `api_server` 改；`verify_strict_auth.py`、报告 新 | `verify_strict_auth` **47/0** | 关：匿名访问本体端点仍通、逐字段与改前一致；开：匿名 401 / 带凭据 200 / read 进 admin 403；AST 扫描 **0 处无界 `await file.read()`** |
| 3 | **令牌吊销/刷新 + 限速**：令牌带 `jti`；管理员吊销后**立即失效**；刷新令牌**轮替**（用后即废，复用留审计）；失败累计超阈值短时封禁，封禁/解除均留痕 | `codes/token_guard.py` 新；`api_server`、`.gitignore` 改 | `verify_token_guard` **39/0** | 真实 HTTP：吊销后旧令牌 401、旧刷新令牌复用 401、突发失败 **429** 序列 `[401,429,429,429,429]`、**封禁期间正常凭据仍 200**（不误伤）、状态目录有界（硬上限裁剪） |
| 4 | **SSO/OAuth2/LDAP 适配**：适配器注册/启用/停用；落地 HS256 + **纯标准库 RS256 公钥验签**（本地密钥，全程离线）；声明映射到角色与租户，缺声明 fail-closed | `codes/sso.py`、`verify_sso.py`、配置样例、报告 新；`api_server` 只加接入点 | `verify_sso` **56/0** | 篡改签名/过期/issuer/audience 不匹配/缺声明 **全拒**；SSO 开时**本地凭据仍 200**；未配置时 `_principal` **逐字段精确相等** |
| — | 收尾：一把跑全门脚本 | `scripts/run_all_gates.py` 新（昨晚） | **8 门全绿** | KB 注册表 28/28 · 商用加固 68/0 · 编排 · 问答信封 · 建议层 · 边界 0 违规 · 单测 78 passed · 评测回归门 0.6280 |

## 兼容性（"只加不松"的硬证据）

- 默认租户下 KB 列表改前改后**集合完全相同（33=33）**
- 老格式令牌 `fotk1` 四段 / `fotk2` 五段**格式不变**，默认仍签发 `fotk1`，老令牌照旧可用（新格式 `fotk3`/`fotkr1` 才带 `jti`）
- 未配置 SSO / 未开严格开关时，`_principal` 与端点行为**逐字段一致**
- 上传、限速的正常路径未被误伤（有并发正常请求仍 200 的实测）

## 方向纪律

- **本体可动但没用上**：四轮都不需要改本体（只在读取路径加租户条件、在接入层加鉴权），所以 `ontology_schema_*.json`、种子、`kbs.json`、`data/` **全部零改动**——这条我逐轮核过。
- **零第三方依赖**：`tenant.py`/`token_guard.py`/`sso.py` 的 import 只有 `os/json/time/hmac/base64/hashlib/sqlite3/contextvars/contextlib/datetime` + 本地模块。
- **零提交**：21 个改动文件全在工作区。

## 没做到 / 不确定项（汇总，逐轮报告里有明细）

1. **MySQL/PostgreSQL 的行级隔离只有代码、没有真库验证**（本机没起 MySQL 服务，驱动也只声明不引用）。
2. **多进程/多实例部署下，吊销名单与限速计数是本地状态文件**，多实例不共享（要共享得上 Redis 或数据库，本轮没做）。
3. **LDAP 只做了接口占位**，未实现未实测（不让为此引依赖）。
4. **SSO 只落地了 JWT 直验**（HS256 + 纯标准库 RS256）；完整的 OAuth2 授权码流程（跳转、换 token、JWKS 在线轮换）未做。
5. 严格鉴权开关**默认关闭**（为了不弄坏前端面板），也就是说**默认部署下本体结构与图仍可匿名读**——上线前要决定是否默认开。
6. 限速阈值、上传上限、吊销名单上限都是**我给的保守默认值 + 依据写在报告里**，属于需要你过一眼的业务参数。
7. `codes/data_loader.py:57` 有一处**未加租户过滤的 SQL 站点**（在本轮允许改的文件范围之外，已记录未改）。

## 建议的提交切分（等你点头）

按轮拆 4 个提交（第 1 轮多租户 / 第 2 轮严格开关+上传 / 第 3 轮令牌与限速 / 第 4 轮 SSO），每个提交信息写明"只加不松 + 自检数字 + 本体零改动"。是否需要同时推远端，也等你一句话。
