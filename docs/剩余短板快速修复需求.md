# 需求：剩余短板快速修复（极值词'贵' + 类型计数支持船型等任意枚举列）

> 基础重构后 9 行业 92%（37/40），剩 3 个长尾短板。其中 2 个是通用词，值得快速纳入自动映射。

## 诊断
1. "最贵的产品"（精加工/波纹管）：numeric_fields 有"价格"→price，但问"最贵"——极值模板识别"最"+"贵"，"贵"字未映射到"价格"字段。极值词表缺"贵"→price
2. "油轮有几艘"（造船）：type_cn2en 有"油轮"，但类型计数模板可能只按设备类型列(device_type)匹配，未覆盖船型(type列)。类型计数模板应支持任意枚举列（设备/产品/船/项目类型的 type/category 列）

## 目标文件
- `E:\open-source\factory-ontology-kit\codes\ontology_qa_v3.py`（极值词表 + 类型计数模板）

## 要求
1. **极值词"贵"映射**：极值模板识别"最贵/最便宜"时，"贵"→price/价格，"便宜"→price（反向）。补"贵"到极值词表
2. **类型计数支持任意枚举列**：类型计数模板不只匹配设备类型(device_type)，应匹配任意枚举列(type/category/category_zh)的值——让"油轮有几艘"（船型type）、"大气治理的项目"（项目type）命中

## 约束
- 极简
- 不破坏现有命中
- 泛化

## 验收
1. data_precision: '最贵的产品'→命中（价格极值）
2. data_ship: '油轮有几艘'→命中（船型计数）
3. data_valve '有多少台设备'→10（不破坏）
4. pytest 29 passed
