# 需求：轻量级调优 — 属性值检索 + 极值字段别名（解决跨行业验证短板）

> 依据跨行业验证（89%命中率）：漏的 2 类是"属性值查询"（不锈钢材质的阀门）和"属性值极值"（保质期最长的产品）。

## 诊断
1. "不锈钢材质的阀门"：种子命中 CF8 产品 + 子图含 bodyMaterial=CF8(304)，但 LLM 说"无法确定"——因为 **schema→prompt 没把"不锈钢"=CF8 的材质同义词告诉 LLM**，LLM 不知道子图里的 CF8(304) 就是"不锈钢"材质
2. "保质期最长的产品"：食品产品有 expiryDays 字段，但规则引擎极值模板字段名不匹配（expiry_days vs expiryDays）

## 目标文件
- `E:\open-source\factory-ontology-kit\codes\graph_rag.py`（schema→prompt 加属性值同义词）
- `E:\open-source\factory-ontology-kit\codes\ontology_qa_v3.py`（极值模板字段别名）

## 要求
### 1. schema→prompt 加属性值同义词（graph_rag）
- 在 _schema_context 注入的属性/实体后，加属性值同义词提示：
  - 如"注意：bodyMaterial 材质的取值有 CF8(304)/CF8M(316)，这些是'不锈钢'材质；用户说'不锈钢'即指这些材质"
- 让 LLM 生成时能把"不锈钢"关联到 CF8
- 极简：可加一个通用提示"属性值可能有中文别名，请结合常识理解（如不锈钢=CF8/304/316）"

### 2. 极值模板字段别名（ontology_qa_v3）
- 极值/过滤模板的字段匹配，补常见别名：expiry_days↔expiryDays、shelf、质保、保质
- 让"保质期最长的产品"极值模板命中

## 约束
- 极简
- 不破坏现有命中（有多少台设备→10等）
- 中文正确，泛化（不硬编码阀门/食品）

## 验收
1. "不锈钢材质的阀门"（data_valve）→ 命中 CF8/CF8M 产品
2. "保质期最长的产品"（data_food_co）→ 极值命中
3. 现有查询不破坏（有多少台设备→10）
4. pytest 29 passed
