# 发布准备包 · factory-ontology 0.3.2 → 0.4.0（2026-09-25）

本文件为**一次开源版本发布的提交前准备包**，只准备，**不 commit / 不 push / 不打 tag**（提交与推送由维护者逐次点头，由主会话执行）。

- 仓库：`factory-ontology`（Apache-2.0 公开库；本地 main，基线 HEAD `5dd7f5b`）
- 工作区范围事实来源：`git status --short` + `git diff --stat`（发布前 57 个文件改动；本准备包另含版本号/CHANGELOG/README 与本文件）
- 硬纪律：未 commit / 未 push / 未打 tag；未改 `data/**` 业务数据；未引新依赖；未 checkout / 回退 / 覆盖任何并发会话改动。
- 收尾门：`python scripts/run_all_gates.py` → **8 门全绿**（下方 §5 真实输出）。

> **路径脱敏说明**：本文件的真实命令输出中，仓库根路径与本机解释器绝对路径已替换为 `<仓库根>` 与 `python`（开源边界自检 A2 规则：库内不得出现开发机绝对路径）。除此之外数字、结论、断言均为原样。

---

## 1. 版本落点表

### 1.1 依据（为什么是 0.4.0）

- 上一个已记录版本 = **0.3.2**（`CHANGELOG.md` 首条，2026-09-16），与工作区改动前所有落点一致。
- 自 0.3.2 以来，本版本线包含**成体系的新增能力**（多租户与行级隔离、严格鉴权开关、令牌吊销/刷新/限速、SSO 适配器、自助建模目录浏览、建模质量与合规派生内核），并新增 3 个后端模块（`tenant.py` / `token_guard.py` / `sso.py`）、多处配置与自检脚本。
- 按 SemVer 在 `0.x` 阶段的惯例：**新增功能 → 次版本号递增（minor）**，破坏兼容 → 主版本（本版全部「只加不松」，无破坏）。因此 `0.3.2 → 0.4.0`（清零补丁位）。
- 与历史线一致：`0.3.0`（重构）→ `0.3.1`（修复级）→ `0.3.2`（功能级，词典资产闭环）。本版改动面明显大于 0.3.2，取 `0.4.0` 合理。

### 1.2 全部落点（文件 → 旧值 → 新值），**已实际改好**

| # | 文件 : 行 | 旧值 | 新值 | 说明 |
|---|---|---|---|---|
| 1 | `codes/run.py` : `__version__` | `0.3.2` | `0.4.0` | **后端唯一事实源**；`api_server._read_app_version()` 与 `web/server/index.js` 的 `/api/ontology/version` 都从这里读出 |
| 2 | `web/package.json` : `version` | `0.3.2` | `0.4.0` | **前端包版本，与后端一致** |
| 3 | `web/package-lock.json` : 根 `version` | `0.2.1`（陈旧） | `0.4.0` | 锁文件根版本（此前落后于 package.json，本次一并拉平） |
| 4 | `web/package-lock.json` : `packages."" .version` | `0.2.1`（陈旧） | `0.4.0` | 同上（lockfileVersion 3 的嵌套根版本） |
| 5 | `codes/e2e_test.py` : 模块 docstring | `v0.3.2` | `v0.4.0` | 端到端测试自述版本 |
| 6 | `codes/e2e_test.py` : 版本断言 | `ck("版本 0.3.2", run.__version__ == "0.3.2")` | `ck("版本 0.4.0", run.__version__ == "0.4.0")` | 与事实源对齐的断言 |
| 7 | `web/server/index.js` : 版本兜底默认值 | `'0.3.1'` | `'0.4.0'` | BFF `/api/ontology/version` 在**读 run.py 失败时**的兜底值（此前停在 0.3.1，属残留漂移） |
| 8 | `README.md` : 徽章 | `version-0.3.2-blue.svg` | `version-0.4.0-blue.svg` | 版本徽章 |
| 9 | `README.md` : 版本节 | `当前版本 **v0.3.2**` + `0.3.x 为当前版本线…` | `当前版本 **v0.4.0**` + `0.4.x 为当前版本线…` | 版本线与说明 |
| 10 | `CHANGELOG.md` | 首条 `## [0.3.2] - 2026-09-16` | 新增 `## [0.4.0] - 2026-09-25` 段（在 0.3.2 之上） | 版本历史（全文见 §2） |

**一致性核对**：后端事实源（#1）= 前端 `package.json`（#2）= 锁文件（#3/#4）= BFF 兜底（#7）= 文档（#8/#9）= CHANGELOG（#10）→ **全部 0.4.0，无一处遗漏**。
**未作改动且已确认无需改动**：`codes/__init__.py`（不存在）、`version.txt`（不存在）、`pyproject.toml`（不存在）、`start.sh`/`start.bat`/`Dockerfile`/`docker-compose.yml`（不含项目版本号）、`web/README.md`（不含版本号）、`codes/export/*.ttl`（其中 `ontology_export.py v0.3.0` 是**导出器**版本，非项目版本，保持不动）。

---

## 2. CHANGELOG 全文（新增 0.4.0 段落，逐条对应真实改动）

> 以下为写入 `CHANGELOG.md` 顶部的新段落原文。每条均可对应到 `git status` 内的真实文件、`git diff` 或 `docs/实验/` 报告出处；未完成项单独列出，不把「计划」写成「已发布」。

<!-- CHANGELOG_0_4_0_BEGIN -->

## [0.4.0] - 2026-09-25

### 商业化鉴权加固 + 自助建模与建模质量（多租户 / 严格鉴权 / 令牌 / SSO / 向导 / 类层次）
> 本版为一次功能级发布：累计工作区 57 个文件改动。八道自检门全绿（`python scripts/run_all_gates.py` → 全部通过（8 个门）），问答评测回归门命中率 **62.8%**、误答率 **18.4%**、编造 **0**。逐项证据见 `docs/实验/` 各报告。

**新增 · 多租户与行级隔离**（`docs/实验/多租户与行级隔离-20260924.md`）
- 新增 `codes/tenant.py`（租户解析：凭据声明 > `X-Tenant-Id` > 默认租户 `default`；`contextvars` 请求级上下文，线程/协程安全）+ `codes/config/tenants.json`（配置驱动租户注册表，缺省只含 `default` 且 `kbs:"*"`，老部署行为逐字段不变）。
- 行级隔离单点：`codes/db_dialect.py` 新增 `tenant_clause` / `select_all_scoped`（缺租户上下文 fail-closed → `TenantContextError`）；`codes/db_loader.py` SQLite 读路径 PRAGMA 探测租户列并强制加租户条件（无租户列的历史表 SQL 与改前逐字节一致）。
- KB 可见性：`codes/kb_registry.py` `list_kbs` / `get_kb(tenant=)` 按租户过滤，未知/越权抛 `TenantDenied`（拒绝而非空列表）。
- 审计双通道带 `tenant`：`codes/audit_chain.py` 每条账本记录与 JSONL 访问日志必带租户。
- 新端点：`GET /api/kbs`（当前租户可见 KB）；`/api/admin/kbs` 增加 `tenant` / `total`；令牌 `fotk2` 携带租户声明。
- 自检 `scripts/verify_tenant.py`：**87/87 通过**（真实 HTTP，两套凭据各打一遍）。

**新增 · 严格鉴权灰度开关 + 上传体积上限**（`docs/实验/严格鉴权开关与上传上限-20260924.md`）
- `FOOD_STRICT_AUTH=1/true/yes/on` 时，本体结构/图/图 SVG/app-config/flows/metrics 等匿名只读端点要求凭据（无/无效 401、`/admin` 非 admin 403）；**默认关，行为逐字节不变**。
- 上传上限 `FOOD_MAX_UPLOAD_MB`（默认 50MB）：`POST /api/admin/upload` 与 `POST /api/knowledge/ingest` 超限返回 **413 且不落盘**，读入改分块累计（AST 扫描 0 处无界 `await file.read()`）。
- 自检 `scripts/verify_strict_auth.py`：**47/47 通过**。

**新增 · 令牌吊销 / 刷新轮替 + 限速防爆破**（`docs/实验/令牌吊销与限速-20260924.md`）
- 新增 `codes/token_guard.py`（零依赖，纯标准库 SQLite；状态目录有界，默认 `FOOD_AUTH_STATE_DIR=<repo>/var/auth_state`）。
- 令牌格式扩展：`fotk3.<role>.<tenant|->.<exp>.<jti>.<sig>`（可吊销访问令牌）、`fotkr1.*`（刷新令牌）；**默认仍签发 `fotk1`，老令牌照旧可用**。
- 新端点：`POST /api/auth/refresh`（刷新即轮替、旧刷新令牌用后即废、复用留审计 `refresh_reuse`）、`POST /api/auth/revoke`（按 `jti` / 按主体批量，吊销后**立即 401**）、`POST /api/auth/ban` / `POST /api/auth/unban`、`GET /api/auth/state`。
- 限速：按「主体」与「来源 IP」两路计数，失败累计超阈值短时封禁（**429** + `Retry-After`），`ban`/`throttle`/`unban` 全留审计；正常凭据在封禁期间不被误伤。
- 自检 `scripts/verify_token_guard.py`：**39/39 通过**。

**新增 · SSO / OIDC(JWT) / LDAP 适配器**（`docs/实验/SSO接入适配-20260924.md`）
- 新增 `codes/sso.py`：适配器注册表（注册/启用/停用）+ `OIDCJWTProvider`（**HS256 + 纯标准库 RS256 公钥验签**）+ `LDAPProvider`（**接口占位，未实现**）。
- `codes/api_server.py` 仅**追加** SSO 入口（`_principal` 末尾）与 `GET /api/auth/sso/status`（admin，只报非敏感配置）；**本地静态 key / 租户 key / 本地令牌永远优先**，SSO 只作兜底。
- 新增配置样例 `codes/config/sso.example.json`（`enabled:false`，全部示例值）；真实密钥配置 `codes/config/sso.json` 已 gitignore。
- 自检 `scripts/verify_sso.py`：**56/56 通过**（篡改签名/过期/issuer/audience 不匹配/缺声明全拒）。

**新增 · 自助建模：目录浏览 + 自动匹配推导**（`docs/实验/自助建模目录浏览与自动匹配-20260925.md`）
- 后端/BFF：`web/server/ontology.js` 新增只读目录枚举与校验（`listDrives`/`listDirs`/`selfmodelCandidates`/`validateDataDir`/`isDangerousPath`，**只返回目录名与元信息，不返回文件内容**）；`web/server/index.js` 新增 `GET /api/fs/drives`、`GET /api/fs/dirs`（**仅本机**，非 127.0.0.1/::1 → 403）、`GET /api/ontology/selfmodel/candidates`、`POST /api/ontology/selfmodel/validate`、`POST /api/ontology/self-onboard`。
- 后端补齐此前缺失的 `POST /api/ontology/self-onboard`（修反向后端断链）；`_resolve_src` / `confirm` 支持仓库外绝对路径并新增危险路径（盘符根/系统目录）拦截。
- 前端：移除 `SelfModelPanel.svelte` 里写死的 `data_valve` 默认，改为从真实数据枚举推导（kb↔目录双向匹配、逐项证据 chip、目录选择器弹窗）；新增封装 `web/src/lib/api.js`。
- 自检 `scripts/verify_selfmodel_browse.py`：**33/33 通过**；`check_chainbreak.py` 反向断链归零。

**新增 · 激活态单一真相源 + 服务/文案修复**（`docs/实验/修A_C组-状态单一真相源与服务-20260925.md`）
- 新增运行态 `codes/config/active_ontology.json`（已 gitignore）作为「当前生效本体」唯一写入点（建库/切库写入，服务启动读取）；`GET /api/kb/active`、`POST /api/kb/active`。
- `/api/standard/compliance`、`/api/standard/export`、`roundtrip`、`quality`、`/api/ask` 缺省 kb 一律跟随当前激活库（不再写死全局 `config/ontology_schema.json`）；`confirm` / `self-onboard` 新增 `data_dir` 占用冲突校验（4091 拒绝）。
- 鉴权 key 来源改为「环境变量优先 → `codes/config/api_keys.json`」，一处未配时返回 503 + 指引（仍 fail-closed）。
- 自检 `scripts/verify_active_ontology.py`：**32/32 通过**（含「重启后端后激活态仍是切换后的库」实测）。

**新增 · 建模质量：类层次 / 实体定义 / 具名子类**（`docs/实验/建模质量-类层次与定义-20260925.md`、`docs/实验/建模质量-扩展描述项-20260925.md`）
- `codes/schema_ontology.py` 新增确定性派生内核：`derive_class_hierarchy` / `apply_class_hierarchy` / `derive_definitions` / `derive_named_subclasses` / `apply_named_subclasses`（纯规则、零 token、同输入同输出）。
- `/api/ontology/suggest` 增返 `hierarchy` / `definitions` / `subclasses`（只读预览，逐条带 `rule` / `evidence`）；`/api/ontology/confirm` 增 `hierarchy_confirmed` / `extensions_confirmed` 硬门（未确认 / 缺依据 / 父实体不存在 → 拒绝落库，**模型只建议、人来确认**）。
- 实测（同源 `ontology_check._check_standard`）：valve 合规度 84.2→**100.0**、实体核心描述项 83.3→**100.0**、类层次 0→**8/38**；food_co 84.2→**100.0**、类层次 0→**7/31**；实体扩展描述项齐备率 75.0→**77.0**。
- 自检 `scripts/verify_standard_hierarchy.py`（39/39）、`scripts/verify_standard_extended.py`（41/41）。

**修复**
- 反向后端断链：补齐 `POST /api/ontology/self-onboard` 后端路由（3.4/第四节）。
- BFF 未代理 `/api/standard/`：合规面板此前拿到首页 HTML → 显示空；`web/server/index.js` 新增该前缀转发并纳入登录门禁（C1）。
- 错误文案骗人：`apiFetch` 对框架级 ≥400 如实透出后端原因（401 → 「未登录或会话已过期，请重新登录」；403/429/5xx → 后端 `detail`），不再被「后端建模失败」兜底掩盖（C2）。
- 漏带 key 即整站 401：key 支持落配置文件 + 未配置时明确指引（C4）。
- `codes/run.py` 新增 `_rel_or_abs()` 替换 `os.path.relpath`：修「自助建模选异盘/仓库外数据目录时 `ValueError: path is on mount C:, start on mount D:`」被掩盖成「按确认 schema 建本体失败」的真 bug。
- `scripts/verify_kb_registry.py` 去掉「恰好 33 个 KB」的脆弱硬编码断言，改为「未缩水 ≥33 且无 `verify_*` 残渣」（D2）。
- `codes/tests/test_api.py` 食品库问答用例显式传 `kb="food"`，与全局激活态解耦，防测试随切库漂移。
- 建模「实体平铺」：自动派生类层次 + 补实体 Definition，消除合规面板两条 major 提示（D1）。
- 前端 `SelfModelPanel.svelte`：候选数据目录由常驻长列表改为**下拉一行**（默认收起、点开展开）；按钮去除 emoji/图标字形，统一站内既有 `.btn` 样式（B1/B2）。

**安全**
- 多租户行级隔离 fail-closed（缺租户上下文即拒，越权拒绝而非返回空）。
- 严格鉴权灰度开关（**默认关闭**）保护 8 条匿名只读业务端点；`/health` 保持匿名探活，`/metrics` 严格模式下需 read 凭据。
- 上传体积上限（默认 50MB），超限 413 且不落盘。
- 令牌吊销下一请求即生效；刷新令牌轮替、复用被拒并留审计；失败限速 429 防爆破。
- SSO 篡改签名 / 过期 / issuer / audience 不匹配 / 缺声明一律拒绝；SSO 开启时本地凭据仍可用。
- `.gitignore` 追加：`var/`（鉴权运行时状态）、`codes/config/sso.json`、`codes/config/active_ontology.json`、`codes/config/api_keys.json`（后两者含运行态/真实 key）。
- 自检脚本脱敏：4 个新增自检脚本内的本地测试 admin/read key 常量由 `devkey-*` 改为中性的 `verify-admin-key` / `verify-read-key`（`scripts/check_boundary.py` A1/A2 违规 0）。

**变更**
- 版本号 `0.3.2 → 0.4.0`，统一 6 处落点（`codes/run.py` `__version__`、`web/package.json`、`web/package-lock.json`（版本字段）、`codes/e2e_test.py` 断言、`web/server/index.js` 兜底版本、`README.md` 徽章与版本节）。
- `codes/export/ontology.ttl` / `ontology.jsonld` / `shapes.ttl`：标准导出物重新生成（含类层次与命名空间治理），为验证时的生成物。
- `codes/config/lexicon_valve.json`：被 `/api/ontology/confirm` 既有流程重生成（新增行业层键，`synonym_map` 有增有减），**非本轮新增逻辑**（见类层次报告 §6.4）。
- `codes/config/ontology_schema_valve.json` / `ontology_schema_food_co.json`：落库类层次、实体 Definition、具名子类（valve 30 条 / food_co 24 条）。
- `codes/config/kbs.json`：新增 KB `food_co`；`food_co.data_dir` 登记为 `data_food_co`（原指向共享目录 `data`，会误扩实体，已纠正）。
- `POST /api/knowledge/ingest` 超限响应码由 200（信封 code=4001）改为 **413**（信封体结构保留）。
- `scripts/eval_qa_baseline.json`：回归基线同步至当前真值（命中率 0.568→**0.628**、误答率 0.296→**0.184**、编造 4→**0**），与已发布的问答信封改造一致。
- `/health` 增加 `apiKeyConfigured` / `backend` 字段（BFF 侧，便于排障，不暴露 key）。
- 前端构建产物重新生成：`web/public/index.html` + `web/public/assets/*`（旧 hash 文件删除，新 hash 文件新增）。
- 令牌格式扩展（`fotk3` / `fotkr1`）为**附加**，默认输出与旧版逐字节一致。

**文档**
- 新增实验报告：`docs/实验/多租户与行级隔离-20260924.md`、`严格鉴权开关与上传上限-20260924.md`、`令牌吊销与限速-20260924.md`、`SSO接入适配-20260924.md`、`商业化鉴权4轮总账-20260925.md`、`自助建模目录浏览与自动匹配-20260925.md`、`修A_C组-状态单一真相源与服务-20260925.md`、`建模质量-类层次与定义-20260925.md`、`建模质量-扩展描述项-20260925.md`、`待修-自助建模前端-20260925.md`（及 `_4轮工作计划-20260924.md`）。
- 新增发布准备：`docs/发布/发布准备-20260925.md`。
- 更新 `CHANGELOG.md`、`README.md`（能力清单 / 快速开始 / 评测基线与自检门 / 已知限制）。

**补记：本版本线内此前已提交、未记入 CHANGELOG 的批次**（仅列事实，SHA 可在 `git log` 核对）
- `9a2c343` 改造第一批：问答评测基线 + KB 注册表与词典分层 + 商用加固。
- `e197585` 改造第二批：问答统一信封 + 编排上半截。
- `c922d17` 改造第三批：模型建议层 + 检查工具与 CI（`scripts/check_boundary.py` / `check_chainbreak.py` / `.github/workflows/ci.yml` self-check job）。
- `b5e8b29` / `5dd7f5b`：`scripts/run_all_gates.py`（一把跑全部门）+ 修其 A2 本机路径。
- `5c3e482` 批7：开源净化 —— 甲方痕迹脱敏（A1/A2 归零）+ 报告。

**本版已知限制 / 未完成项**（据各报告「没做到」章节如实汇总，未粉饰）
1. 严格鉴权开关 `FOOD_STRICT_AUTH` **默认关闭** —— 默认部署下本体结构与图仍可匿名读；上线前需决定是否默认开；且该开关是**路径白名单**（新增匿名端点需手工加进 `_STRICT_READ_PATHS` 才受保护）；`/admin` HTML 在严格模式下浏览器无法直接打开（顶层导航不带请求头）。
2. **多进程/多实例部署下，令牌吊销名单与限速计数是本地状态文件，多实例不共享**（要共享需引入 Redis/数据库，本版未做）。
3. **MySQL / PostgreSQL 的行级隔离只有代码，无真库验证**（本机未起真库，驱动未安装）；且 mysql/pg 侧不做租户列自动探测（需配置显式 `tenant_col`）。
4. **LDAP 只做接口占位，未实现、未实测**。
5. **SSO 只落地 JWT 直验（HS256 + RS256）**；完整 OAuth2 授权码流程（跳转 / 换 token / JWKS 在线轮换）未做。
6. **等效类（EquivalentClass）两库均不可提升** —— 词典/别名表无「被建模成实体」的真同义实体，故不伪造；实体扩展描述项齐备率因此封顶 77%（有真实依据下的可达值，数学上限约 81%，不填假关系）。
7. **具名子类未做实例级归类**（未生成 `:Valve_products_闸阀` 的实例 `rdf:type`）；且 `--kb` 只落 valve / food_co，历史测试残渣库（valve2/valve3/valve9）未处理。
8. **`codes/data_loader.py:57` 有一处未加租户过滤的 SQL 站点**（不在允许改动的文件范围内，已登记未改）。
9. **`X-Tenant-Id` 头本身不鉴权**：无凭据的既有路由仍可用该头自选租户；真实部署建议前置网关鉴权或只信凭据声明。
10. **default 租户等同超管**（`kbs:"*"` 可见全部 KB）—— 这是「老部署行为不变」的锚点，真实多租户部署须收口其 scope。
11. **外部绝对路径的「建本体」未端到端实测**；`self-onboard` 后端路由未做真实上传建模端到端实测（避免在真机跑真实建模写产物）；`/api/fs/dirs` 不拦危险路径（仅 `selfmodel/validate` 拦）。
12. **BFF 门禁「单企业收敛」语义未改**：经 BFF 的请求会把激活库拉回登录用户绑定的 kb（一企业一库的既有产品语义）；仅通过后端 API 切库而不同步 `user.kb` 时，下次经 BFF 的请求会回退。
13. **`/api/stats` 分组统计 tie 顺序既有非确定性**（同版本两进程 raw 结果不同）—— 与本版改动无关，但影响「逐字段相等」类断言。
14. **超大 body 的 ASGI 层 spool 不在本改动可控范围**（Starlette 进入端点前先落 `SpooledTemporaryFile`）；未做自定义 ASGI 中间件级流式拒绝。
15. 限速阈值、上传上限、吊销名单上限均为**保守默认值**，属需业务侧复核的参数。

<!-- CHANGELOG_0_4_0_END -->

---

## 3. README 改动摘要

与代码现状对齐，**不夸大、不写未实现能力**，简洁符合开源惯例。具体改动：

| # | 位置 | 改动 | 依据 |
|---|---|---|---|
| 1 | 顶部徽章 | `version-0.3.2` → `version-0.4.0` | §1 版本落点 |
| 2 | 核心组件 · 周边能力段 | `config/`（… + 多租户注册表）→ `config/`（… + 多租户/SSO 配置） | 新增 `config/tenants.json` / `config/sso.example.json` |
| 3 | 核心组件 · **新增两段** | 新增「商业化鉴权与自助建模（v0.4.0）」模块清单（`tenant.py` / `db_dialect.py` / `db_loader.py` / `token_guard.py` / `sso.py` / `audit_chain.py` / `schema_ontology.py` 派生内核 / 配置样例）；新增「自检脚本」段（`run_all_gates.py` / `check_boundary.py` / `check_chainbreak.py` / `verify_*.py`） | `git status` 新增文件 |
| 4 | REST API · 工程化能力段 | 扩写：key 可走环境变量或 `config/api_keys.json`；多租户与行级隔离（`config/tenants.json`）；严格鉴权开关；上传上限 413；令牌吊销/刷新/限速端点；SSO 适配器；哈希链审计账本 | §2 各新增段 |
| 5 | **新增章节**「评测基线与自检门」（置于「实测数据」与「模型配置」之间） | 问答评测集口径（8 KB / 331 问 / 五类问法 / 未命中≠误答≠编造）+ 基线数字（命中 **62.8%**、误答 **18.4%**、编造 **0**）+ 八道门清单与 `run_all_gates.py` 用法 + 「v0.4.0 当前 8 门全绿」 | `scripts/eval_qa_baseline.json`、§5 门输出 |
| 6 | 诚实边界 | 新增两条：**鉴权**（严格开关默认关 / 多实例状态不共享 / MySQL 未真库验证 / LDAP 占位 / SSO 未做授权码流程）与**建模合规上限**（扩展描述项可达 77%，等价类不可提升是诚实结论，具名子类未做实例级归类） | §2 已知限制项 |
| 7 | 版本节 | `v0.3.2` → `v0.4.0`；版本线说明改为 `0.4.x 为当前版本线（多租户与行级隔离 + 鉴权加固 + 令牌吊销/刷新与限速 + SSO 适配 + 自助建模目录浏览/自动匹配 + 建模质量与合规治理）` | §1 |
| 8 | 阀门实测输出标注 | `实测输出（v0.3.1…）` → 补注「数字仍同口径可用」（保留历史版本标注，不改写历史输出） | 开源惯例，不篡改历史实测 |

**未改夸大的点**：未把「多租户/鉴权」写成「生产级多租户 SaaS」，未把「SSO」写成「完整 OAuth2」，未把「合规度 100%」写成对国标全项达标——均按报告口径「本仓库轻量 schema，非 W3C OWL 推理本体」如实表述。

---

## 4. 脱敏与合规自检（真实输出）

### 4.1 `python scripts/check_boundary.py`

```
============================================================================
开源边界自检报告  check_boundary.py
============================================================================
仓库根      : <仓库根>
扫描文件数  : 499 (git 跟踪)
已声明依赖  : ['docx', 'edge_tts', 'fastapi', 'faster_whisper', 'fitz', 'httpx', 'multipart', 'numpy', 'openpyxl', 'pdfplumber', 'psycopg2', 'pydantic', 'pymupdf', 'pymysql', 'pytest', 'requests', 'sounddevice', 'uvicorn']

----------------------------------------------------------------------------
A1 真实企业名/客户名命中: 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
A2 本机/内部绝对路径命中: 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
B1 import 开源引擎: 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
B2 import 不存在的模块: 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
C1 未声明的第三方依赖(核心路径): 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
C2 核心引擎文件引入了第三方: 0
----------------------------------------------------------------------------

----------------------------------------------------------------------------
[参考] 模块级第三方 import 共 6 处 (已声明 6 / 未声明 0)
----------------------------------------------------------------------------
  [已声明] codes/api_server.py:52  fastapi
  [已声明] codes/api_server.py:53  fastapi
  [已声明] codes/api_server.py:54  pydantic
  [已声明] codes/tests/test_ontology_check.py:15  pytest
  [已声明] codes/tests/test_plugin_framework.py:13  pytest
  [已声明] codes/model_llm.py:18  requests

----------------------------------------------------------------------------
[参考] 核心引擎文件纯标准库核对 (=24 个)
----------------------------------------------------------------------------
  检到核心文件 24/24; 违规 0

[INFO] 惰性 import 命中开源引擎黑名单(函数/try 内, 不计违规): 2
  codes/event_bus.py:319  import solo
  codes/tests/test_event_bus.py:52  import solo

============================================================================
结论: 边界自检通过 (违规 0)
============================================================================
```

**目标达成**：A1 真实企业名 = **0**、A2 本机绝对路径 = **0**；退出码 0。

### 4.2 `python scripts/check_chainbreak.py`

```
仓库根            : <仓库根>
后端路由数        : 53  (codes/api_server.py)
BFF 对外路由      : 52  (web/server/index.js)
ontology.js 转发函数 : 49
前端引用路径      : 57  (web/, 排除 node_modules)
页面层文件        : 7

[汇总] 有前端入口: ...   仅BFF转发(半链): ...   断链: 28
...
[反向汇总] BFF 指向不存在端点: 0

============================================================================
结论: 有问题 —— 正向断链 28 项, 反向断链 0 项
============================================================================
```

**如实说明**：该脚本**退出码 1**（正向断链 28 项）。这 28 项全部是**只由页面层/后端直连、无 SPA 入口**的端点（如 `/api/admin/rebuild`、`/api/admin/sync`、`/api/auth/*`、`/api/kb/active`、`/api/version`、`/health`、`/metrics` 等），属**既有的「后端先于前端」缺口**（改动前为 26 项，本版因新增 `POST /api/kb/active`、`GET /api/kb/active`、`/api/auth/*` 等端点 +2），**非本次引入回归**。本版内**反向断链（BFF 指向不存在后端端点）= 0**，即 `/api/ontology/self-onboard` 反向断链已修掉。
> 该脚本**不在 8 道门之内**（8 门定义见 `run_all_gates.py`），故不影响 §5 全绿。

### 4.3 本轮新增/修改文件的敏感信息扫描（人工可核）

扫描范围 = `git status --short` 列出的全部改动文件（排除 `web/public/assets` 构建产物）。命令均在仓库根执行（`cd <仓库根>` 已省略）：

**① 本机绝对路径（开发机盘符路径 `D:\` 系列、个人目录、本机解释器目录、`/d/...` 形式）**
```bash
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "D:[/\\\\]factory-ontology|C:[/\\\\]Users|C:[/\\\\]Python312|/d/factory-ontology" "$f" | sed "s|^|$f:|"
done
```
**结果：无输出（命中 0）。** ✅

**② 甲方/客户名与真实企业名关键词**（清单同 `check_boundary.py` A1 的企业名黑名单，此处**不逐一写出被禁词**以免自命中）
```bash
# <COMPANY_BANLIST> = check_boundary.py BANNED_COMPANIES 清单（能源/石化/通信/家电/汽车等行业头部企业名）
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "<COMPANY_BANLIST>" "$f" | sed "s|^|$f:|"
done
```
**结果：无输出（命中 0）。** ✅（与 `check_boundary.py` A1=0 一致）

**③ 内网 IP / 非回环地址**
```bash
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.|10\.[0-9]{1,3}\.[0-9]{1,3}\." "$f" | sed "s|^|$f:|"
done
```
**结果：命中 0 处内网地址**；仅出现回环 `127.0.0.1` / `::1`（自检脚本起临时服务、BFF 本机限定判定所需，属正常）。

**④ 真实人名 / 联系方式（手机号、邮箱、微信、电话）**
```bash
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "[0-9]{11}|@(qq|163|gmail|126)\.|微信号|电话[:：]" "$f" | sed "s|^|$f:|"
done
```
**结果：命中 0 处真实联系人/联系方式**（`codes/config/kbs.json` 中的 `factory_multi_1789459752280` 为运行时生成的 KB 标识串，非联系方式）。

**⑤ 密钥 / 测试 key 扫描（含本轮新增自定义扫描）**
```bash
# 模式串含具体测试 key 名（已按脱敏规则以 <TEST_KEY_PAT> 代指，实际为「devkey- + 角色」形式）
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "<TEST_KEY_PAT>|sk-[A-Za-z0-9]{10,}|AKIA[0-9A-Z]{12,}" "$f" | sed "s|^|$f:|"
done
```
**修复前结果（命中 4 文件 / 8 行，值已打码）：**
```
scripts/apply_standard_extensions.py:41:ADMIN_KEY = "<TEST_KEY_ADMIN>"
scripts/apply_standard_extensions.py:42:READ_KEY  = "<TEST_KEY_READ>"
scripts/verify_active_ontology.py:39:ADMIN_KEY = "<TEST_KEY_ADMIN>"
scripts/verify_active_ontology.py:40:READ_KEY  = "<TEST_KEY_READ>"
scripts/verify_standard_extended.py:44:ADMIN_KEY = "<TEST_KEY_ADMIN>"
scripts/verify_standard_extended.py:45:READ_KEY  = "<TEST_KEY_READ>"
scripts/verify_standard_hierarchy.py:41:ADMIN_KEY = "<TEST_KEY_ADMIN>"
scripts/verify_standard_hierarchy.py:42:READ_KEY  = "<TEST_KEY_READ>"
```
**处置（标识类脱敏，只改值不改逻辑）**：4 个新增自检脚本内的本地测试 admin/read key 常量改为中性的 `verify-admin-key` / `verify-read-key`——脚本本身在「启动临时服务」与「客户端请求」两处使用**同一常量**，故值一致即可，行为不变；这 4 个脚本**不在 8 道门内**，不影响门。

**修复后复扫结果：**
```bash
for f in $(git status --short | awk '{print $2}'); do
  [ -f "$f" ] && grep -nE "<TEST_KEY_PAT>" "$f" | sed "s|^|$f:|"
done
# 无输出（命中 0）
```
**结果：命中 0。** ✅

> 说明：为符合「文档不得出现该测试 key 字面值」的脱敏规则，本报告内该 key 一律以上述占位符代指，**不写出真实字面值**。

> 说明：本报告文件（`docs/发布/发布准备-20260925.md`）本身**不含** `devkey-*`、甲方名、内网地址与本机绝对路径（已按 §上方路径脱敏说明处理），并已复跑 `check_boundary.py` 确认 A1/A2=0。

**⑥ 其他敏感物（如实列出，判定为可保留）**
- `codes/config/sso.example.json`：全部为**示例值**（`enabled:false`、`secret:"REPLACE_WITH_SHARED_SECRET_EXAMPLE"`、`issuer: https://sso.example.com/...`），非真实密钥/域名。
- `scripts/verify_sso.py`：含 `TEST_RSA_D_HEX`（**自检用的测试 RSA 私钥**，用于离线验证 RS256 验签链路），非生产密钥。
- `codes/config/tenants.json`：仅 `default` 租户（`kbs:"*"`），无真实客户信息。
- 上述三项判定为**可保留**（示例/测试用途，无真实凭据泄露面）。

---

## 5. 8 道门真实输出

命令：`python scripts/run_all_gates.py`（收尾复跑，含我的版本号/脱敏改动之后）

```
==========================================================================
全部门一览  仓库: <仓库根>
==========================================================================
  [PASS] KB 注册表与词典分层                  rc=0  自检结果: 28/28 通过
  [PASS] 商用加固（鉴权/方言/审计链）              rc=0  通过 68 项, 失败 0 项
  [PASS] 编排上半截（流程/控制流/预设）             rc=0  全部通过 ✅
  [PASS] 问答信封与未命中                     rc=0  结果: PASS  题331 可答250 命中189 无答案81（hit=true 均有 evidence；无答案零编造）
  [PASS] 模型建议层                        rc=0  结果: PASS  无模型时逐字段一致率 100.0000%；启用时只加 advisory，hit/answer 零影响
  [PASS] 边界自检（甲方痕迹/依赖）                rc=0  结论: 边界自检通过 (违规 0)
  [PASS] 仓库单测                         rc=0  78 passed, 1 skipped, 2 warnings in 8.03s
  [PASS] 问答评测基线（回归门）                  rc=0  基线落盘: <仓库根>/scripts/eval_qa_baseline.json
--------------------------------------------------------------------------
全部通过（8 个门）
```

**结论：8 门全绿，退出码 0。** ✅（注：评测门会把 `scripts/eval_qa_baseline.json` 重新落盘为当前真值，故该文件在提交前处于「已更新」状态，见 §2 变更条目。）

---

## 6. 提交拆分建议（给主会话执行）

> 原则：**按主题分组、每组内部自洽**；下表标注「必须同组」的文件——若拆散会导致中间态跑不过门（如 `api_server.py` 同时承载多租户/严格鉴权/令牌/SSO/自助建模后端，拆碎会使某组缺依赖）。
> **执行方式**：请用**逐文件 `git add <path>`**，**不要 `git add -A` / `git add .`**（工作区含无关产物，见 §7 警告）。

### 组 1 · 多租户与鉴权加固（第 1 轮）
- 文件：`codes/tenant.py`（新）、`codes/config/tenants.json`（新）、`codes/db_dialect.py`、`codes/db_loader.py`、`codes/audit_chain.py`、`codes/kb_registry.py`、`scripts/verify_tenant.py`（新）
- **必须同组**：`codes/api_server.py` 的租户解析/中间件/KB 守卫部分与 `codes/tenant.py` 强耦合 → **`api_server.py` 需整文件纳入本组或以下某组**（见组间说明）。
- 建议 message：`feat(auth): 多租户与行级隔离 —— 租户解析 + 数据层强制租户条件(fail-closed) + KB 按租户可见 + 审计带 tenant`

### 组 2 · 严格鉴权开关与上传上限（第 2 轮）
- 文件：`scripts/verify_strict_auth.py`（新）
- 代码：`codes/api_server.py`（`FOOD_STRICT_AUTH` / `FOOD_MAX_UPLOAD_MB` / `_read_upload_capped` / 413 路径）
- 建议 message：`feat(auth): 严格鉴权灰度开关(默认关) + 上传体积上限(默认 50MB, 超限 413 不落盘)`

### 组 3 · 令牌吊销 / 刷新 / 限速（第 3 轮）
- 文件：`codes/token_guard.py`（新）、`scripts/verify_token_guard.py`（新）、`.gitignore`（追加 `var/`）
- 代码：`codes/api_server.py`（`fotk3`/`fotkr1` / `/api/auth/refresh|revoke|ban|unban|state` / 限速）
- 建议 message：`feat(auth): 令牌吊销(立即生效) + 刷新轮替(用后即废) + 限速防爆破(429)，默认仍签发 fotk1`

### 组 4 · SSO / OIDC / LDAP 适配（第 4 轮）
- 文件：`codes/sso.py`（新）、`codes/config/sso.example.json`（新）、`scripts/verify_sso.py`（新）、`.gitignore`（追加 `codes/config/sso.json`）
- 代码：`codes/api_server.py`（`_principal` 末尾 SSO 入口 + `GET /api/auth/sso/status`）
- 建议 message：`feat(auth): SSO/OIDC(JWT, HS256+RS256 纯标准库) 可选适配器，本地凭据优先；LDAP 接口占位`

### 组 5 · 激活态单一真相源 + 服务/文案修复（A/C 组）
- 文件：`scripts/verify_active_ontology.py`（新）、`.gitignore`（追加 `active_ontology.json` / `api_keys.json`）、`codes/ontology_check.py`
- 代码：`codes/api_server.py`（`_read_active`/`_active_kb`/`/api/kb/active`/compliance·export 跟激活库/data_dir 冲突/503 指引）、`web/server/ontology.js`、`web/server/index.js`、`web/src/lib/api.js`
- 建议 message：`fix(state): 激活态单一真相源(active_ontology.json) + 合规/导出/ask 跟随当前库 + 错误文案不吞真因 + key 落配置`

### 组 6 · 自助建模目录浏览与自动匹配
- 文件：`scripts/verify_selfmodel_browse.py`（新）
- 代码：`web/server/ontology.js`、`web/server/index.js`、`web/src/lib/api.js`、`web/src/components/SelfModelPanel.svelte`、`codes/api_server.py`（`POST /api/ontology/self-onboard` 补路由 + 外部路径/危险路径拦截）
- 建议 message：`feat(selfmodel): 数据目录浏览(仅本机) + kb↔目录自动匹配推导 + 补 self-onboard 后端路由(修反向断链)`

### 组 7 · 建模质量与合规（类层次 / 定义 / 具名子类）
- 文件：`codes/schema_ontology.py`、`scripts/verify_standard_hierarchy.py`（新）、`scripts/verify_standard_extended.py`（新）、`scripts/apply_standard_extensions.py`（新）
- 数据/配置（**同组**，否则合规数字与自检不符）：`codes/config/ontology_schema_valve.json`、`codes/config/ontology_schema_food_co.json`（新）、`codes/config/lexicon_food_co.json`（新）、`codes/config/kbs.json`（新增 food_co + 纠正 data_dir）、`codes/config/lexicon_valve.json`（被 confirm 流程重生成）、`codes/export/ontology.ttl` / `ontology.jsonld` / `shapes.ttl`（重建导出物）
- 代码：`codes/api_server.py`（`hierarchy_confirmed`/`extensions_confirmed` 硬门 + suggest 增返）
- 建议 message：`feat(modeling): 自动派生类层次/实体定义/具名子类(规则+人确认)，合规度 valve/food_co 84.2→100`

### 组 8 · 前端面板与构建产物
- 文件：`web/src/components/SelfModelPanel.svelte`、`web/src/lib/api.js`、`web/public/index.html`、`web/public/assets/index-D7OifjU_.js`（新）、`web/public/assets/index-Dz1jDqjx.css`（新）、`web/public/assets/index-r7_rE_FG.js`（删）、`web/public/assets/index-CjaW7skw.css`（删）
- **必须同组**：`index.html` 与新旧 hash 资源**必须一起提交**（否则引用断链、页面白屏）。
- 说明：若组 6/组 7 已含同名源文件，可将本组并入对应组；**构建产物与源文件同组**最稳。
- 建议 message：`build(web): 重新构建前端产物（自助建模折叠候选 + 层次/定义确认卡片 + 去 emoji 按钮）`

### 组 9 · 门禁与测试修正
- 文件：`scripts/verify_kb_registry.py`、`codes/tests/test_api.py`、`scripts/eval_qa_baseline.json`
- 建议 message：`test(gates): 去掉 KB 数量硬编码断言 + 问答用例与全局激活态解耦 + 回归基线同步真值(0.568→0.628)`

### 组 10 · 文档与版本号
- 文件：`CHANGELOG.md`、`README.md`、`codes/run.py`、`web/package.json`、`web/package-lock.json`、`codes/e2e_test.py`、`docs/实验/*.md`（10 份新报告 + `_4轮工作计划-20260924.md`）、`docs/发布/发布准备-20260925.md`
- **必须同组**：`codes/run.py`（版本事实源）与 `web/package.json` / `web/package-lock.json` / `codes/e2e_test.py`（版本断言）**必须同组**——否则出现「前后端版本号不一致」的中间态。
- 建议 message：`docs(release): 0.4.0 —— CHANGELOG/README 更新 + 版本号统一 6 处 + 实验报告归档`

### 组间「必须同组 / 不能拆散」要点（避免中间态跑不过门）
1. **`codes/api_server.py` 是单文件多主题**：`tenant` / 严格鉴权 / 令牌 / SSO / 自助建模 / 激活态 的改动都在这一个文件里。**要么把它整文件放进某一组**（且该组 message 覆盖多主题），**要么按主题分多次提交时承认前几次提交的 `api_server.py` 尚不完整**——但 `run_all_gates.py`（尤其 `verify_hardening.py`）会读该文件，**建议把 `api_server.py` 与「组 1~组 7」一并作为一个较大提交**，或按组逐个 `git add -p`（分段暂存）后每组各自能过门。最稳妥：**按主题拆 message，但把 `api_server.py` 的全量改动放在组 1 之后、组 7 之前的任一「主组」里一次性提交**。
2. **前端构建产物 `web/public/index.html` + `web/public/assets/*`**：新旧 hash 文件与 index.html 必须同一提交。
3. **合规数字依赖落盘 schema**：`ontology_schema_valve.json` / `ontology_schema_food_co.json` / `lexicon_*` / `kbs.json` / `codes/export/*` 与 `schema_ontology.py` / `verify_standard_*.py` 必须同组，否则门/合规面板数字对不上。
4. **版本号 6 处落点**（§1.2 表）：同组，否则中间态版本漂移。
5. **`.gitignore`** 的 4 行新增（`var/`、`sso.json`、`active_ontology.json`、`api_keys.json`）跨组 1/3/4/5，可随任一组带入，但**必须在本批内提交**（否则运行态文件有被误提交风险）。

---

## 7. 没做到 / 不确定项（本准备包自身）

1. **未 commit / 未 push / 未打 tag**（用户明确要求；提交与推送由主会话逐次执行）。
2. **`check_chainbreak.py` 退出码为 1（正向断链 28 项）**：均为既有「后端先于前端」缺口（改动前 26 项，本版 +2），**非本次引入**，且该脚本**不在 8 道门内**。若要清零需前端补入口，属后续工作。
3. **CHANGELOG 的「补记批次」依赖 `git log` 事实**：`9a2c343`/`e197585`/`c922d17`/`b5e8b29`/`5dd7f5b`/`5c3e482` 均在 0.3.2（2026-09-16）之后、未记入 CHANGELOG；我已按「只列事实 + SHA 可核」补记，**未替其编造细节**。若维护者认为这些批次不应并入 0.4.0 叙述，可删除该小节。
4. **`web/public/assets` 构建产物由既有 `npm run build` 生成**：本准备包**未重跑 build**，仅按 `git status` 现状登记（新 hash 文件新增、旧 hash 文件删除）。若维护者要确保产物与当前源码逐字节一致，建议提交前重跑一次 `cd web && npm run build`。
5. **`scripts/eval_qa_baseline.json` 处于「门重跑后自动落盘」的状态**：这是 `run_all_gates.py` 评测门的固有行为（每次跑都重写）。基线值 0.568→0.628 的真实来源是**已提交**的问答信封改造（属本版本线内），此处为基线文件补齐，**非本版新增引擎改动**。
6. **`unsloth_compiled_cache/`（未跟踪）**：工作区存在该目录，内容为**本地微调编译缓存**，与本发布**无关**。**警告：切勿 `git add -A`** —— 建议提交前将其加入 `.gitignore` 或删除。
7. **`docs/商业化调研/` 两份未跟踪文档**（`Laya与JEV调研-20260925.md`、`Laya与JEV调研-决策模型口径-20260925.md`）：属**另一会话的调研产物，非本轮发布内容**。本准备包**未触碰**；建议**不并入本次发布提交**，或另行单独提交。
8. **人工扫描的边界**：§4.3 的扫描基于正则关键词（甲方名/联系方式/内网/密钥/路径），**不能保证覆盖所有可能的敏感表述**（如未列入清单的甲方别名、业务敏感数值）。已用 `check_boundary.py`（工具化 A1/A2）交叉验证为 0；如需更强保证，建议维护者在提交前对 `git diff` 快速通读一遍。
9. **版本号只升不降、未动 `data/**`、未引新依赖** —— 已遵守；`data/` 目录本准备包零改动（新增 KB 数据目录 `data_food_co/` 由另一会话产出，本包未触碰其内容）。
10. **README 未新增目录树（tree）章节**：现有 README 以「核心组件表 + 周边能力段」承载目录/模块说明，本包沿用该体例新增两段，**未强行改成 tree 结构**（与既有开源文风保持一致）。

---

*本文件由发布准备子任务产出，仅准备，不代表已发布。*
